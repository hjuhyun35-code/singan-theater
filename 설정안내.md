# 설정 안내

키를 하나씩 채워 넣는 순서입니다. **위에서부터 순서대로** 하세요.
중간에 막히면 `설정확인.bat` 을 실행하면 어디가 문제인지 알려줍니다.

키를 받을 때마다 **`.env` 파일**(메모장으로 열면 됩니다)의 해당 줄 `=` 뒤에 붙여넣으세요.
따옴표는 넣지 않습니다.

```
ALADIN_TTB_KEY=ttbmyname1234001      ← 이렇게 (O)
ALADIN_TTB_KEY="ttbmyname1234001"    ← 따옴표 넣으면 안 됨 (X)
```

---

## 1단계 — 초안 만들기 (약 15분)

이 두 개만 있으면 발행은 못 해도 **초안이 어떻게 나오는지 바로 볼 수 있습니다.**
Meta 설정은 오래 걸리니, 먼저 여기까지 해보고 결과물이 마음에 드는지 판단하세요.

### 1-1. 알라딘 API 키 — `ALADIN_TTB_KEY`

책 정보를 가져오는 곳입니다. 무료이고 하루 5,000번까지 됩니다.

1. 알라딘에 로그인 → https://www.aladin.co.kr/ttb/wblog_manage.aspx
2. **TTB 키 발급** 신청 (블로그/사이트 주소를 물으면 본인 인스타나 쓰레드 주소를 넣으면 됩니다)
3. 발급된 키(`ttb`로 시작하는 문자열)를 복사 → `.env` 의 `ALADIN_TTB_KEY=` 뒤에 붙여넣기

> **제휴(어필리에이트) 안내**
> 같은 페이지에서 제휴 회원 신청도 할 수 있습니다. 신청해두면
> ① 책 표지 이미지를 쓰는 근거가 생기고 ② 링크로 책이 팔리면 수수료를 받습니다.
> 신청 후 받은 파트너 ID를 `config.json` 의 `알라딘_파트너ID` 에 넣으세요.

### 1-2. Claude API 키 — `ANTHROPIC_API_KEY`

요약 문구를 쓰는 데 씁니다. **한 달에 1,000원도 안 나옵니다** (하루 3건 기준).

1. https://console.anthropic.com/settings/keys 접속 후 가입/로그인
2. **Create Key** → 이름은 아무거나 (예: book-bot)
3. 나온 키(`sk-ant-`로 시작)를 복사 → `.env` 의 `ANTHROPIC_API_KEY=` 뒤에 붙여넣기
   - **이 화면을 닫으면 키를 다시 볼 수 없습니다.** 바로 붙여넣으세요.
4. 결제 수단 등록이 필요합니다. Billing 메뉴에서 $5 정도만 충전해도 몇 달 씁니다.

### 1-3. 확인

`설정확인.bat` 을 더블클릭하세요. 알라딘과 Claude가 **[정상]** 이면 성공입니다.

이제 `실행.bat` 을 더블클릭하면 초안이 만들어지고 브라우저에 승인 화면이 뜹니다.
**아직 어디에도 올라가지 않습니다.** 발행 버튼은 눌러도 실패합니다(2단계를 안 했으니까요).

---

## 2단계 — 실제로 올리기 (약 1시간)

여기서부터는 Meta 개발자 사이트를 다뤄야 하고, 화면이 영어입니다.
**혼자 하지 말고 저와 화면 보면서 같이 하는 걸 권합니다.** 막히면 물어보세요.

### 2-1. 이미지 호스팅 — `IMGBB_API_KEY`

쓰레드와 인스타는 내 컴퓨터의 이미지 파일을 직접 못 받습니다.
반드시 인터넷에 올라간 공개 주소가 있어야 해서 필요합니다. 무료입니다.

1. https://api.imgbb.com/ 접속 → **Get API key** → 가입
2. 받은 키를 `.env` 의 `IMGBB_API_KEY=` 뒤에 붙여넣기

### 2-2. 인스타그램 계정 준비

인스타 앱에서:
**설정 → 계정 유형 및 도구 → 프로페셔널 계정으로 전환** → **크리에이터** 선택

- **비공개 계정이면 안 됩니다.** 반드시 공개로 두세요.
- 페이스북 페이지는 만들 필요 없습니다. (예전 방식은 필요했지만 지금은 아닙니다)

### 2-3. 쓰레드용 Meta 앱 만들기

1. https://developers.facebook.com/apps 접속 → 페이스북 계정으로 로그인
   (개발자 등록을 처음 한다면 전화번호 인증을 한 번 합니다)
2. **앱 만들기(Create App)** 클릭
3. 앱 이름은 아무거나 (예: `book-bot-threads`)
4. 사용 사례(Use case)에서 **"Access the Threads API"** 선택
5. 만들어진 앱에서 왼쪽 메뉴 **Use cases → Access the Threads API → 사용자 지정(Customize)**
6. 권한 추가: **`threads_basic`**, **`threads_content_publish`** 두 개 (Add 버튼)
7. **앱 역할(App roles) → 역할(Roles)** 에서 내 쓰레드 계정을 **테스터로 추가**
   - 그다음 **쓰레드 앱**을 열어 → 설정 → 웹사이트 권한 → 초대 수락
   - 이 단계를 빠뜨리면 토큰이 안 나옵니다
8. **Use cases → Access the Threads API → 설정(Settings)** 탭 → 아래로 스크롤 →
   **사용자 토큰 생성기(User Token Generator)** → 내 계정 옆 **Generate Access Token** 클릭
   - **토큰은 한 번만 보여줍니다.** 바로 복사하세요.
9. 같은 앱의 **앱 설정 → 기본 설정**에서 **Threads 앱 ID / Threads 앱 시크릿**을 복사해
   `.env` 의 `THREADS_APP_ID` / `THREADS_APP_SECRET` 에 넣기
   - 주의: 앱 ID가 두 개 보입니다. 반드시 **Threads 쪽** 값을 쓰세요.
10. 복사한 토큰을 아래 명령으로 등록합니다 (검은 창에 붙여넣고 Enter):

```bash
cd "C:\Users\garii\OneDrive\문서\바탕 화면\code\book-bot" && .venv\Scripts\python.exe tools\setup_token.py threads 여기에_복사한_토큰
```

성공하면 60일짜리 토큰과 계정 ID가 `.env` 에 자동 저장됩니다.

### 2-4. 인스타그램 — **이미 만들어 둔 앱을 그대로 씁니다**

`@theglassnegative` 를 세팅할 때 만든 **`glassnegative-bot`** 앱에 필요한 권한
(`instagram_business_basic`, `instagram_business_content_publish`)이 이미 다 켜져 있습니다.
**새 앱을 만들지 마세요.** 책 계정을 테스터로 추가하고 토큰만 뽑으면 됩니다.

1. https://developers.facebook.com/apps → 앱 **`glassnegative-bot`** 선택
2. **앱 역할 → 역할 → 더 보기 → Instagram 테스터** → **책 계정 아이디 추가**
3. **책 계정으로** 인스타에 로그인한 상태에서 아래 주소로 직접 접속 →
   **테스터 초대 수락**

   ```
   https://www.instagram.com/accounts/manage_access/
   ```

   > ⚠️ **초대는 알림으로 오지 않습니다.** 이 주소로 직접 들어가야 보입니다.
   > 이 수락을 건너뛰고 다음 단계로 가면 「개발자 역할 권한 부족」 오류가 납니다.

4. 앱 → **Instagram 로그인이 포함된 API → 2. 액세스 토큰 생성 → 계정 추가**
   → **반드시 책 계정으로 로그인** (개인 계정으로 로그인하면 거기에 글이 올라갑니다)
5. **Generate token** → 토큰 복사 (한 번만 보여줍니다)
6. **앱 설정 → 기본 설정**에서 **Instagram 앱 ID / 시크릿**을 복사해
   `.env` 의 `INSTAGRAM_APP_ID` / `INSTAGRAM_APP_SECRET` 에 넣기
7. 복사한 토큰을 등록:

```bash
cd "C:\Users\garii\OneDrive\문서\바탕 화면\code\book-bot" && .venv\Scripts\python.exe tools\setup_token.py instagram 여기에_복사한_토큰
```

제대로 됐는지는 이 주소로도 확인할 수 있습니다. **책 계정 아이디**가 나와야 합니다.

```
https://graph.instagram.com/me?fields=id,username&access_token=토큰
```

> 인스타 ID는 두 종류입니다. Meta 화면에 보이는 번호와 `graph.instagram.com/me` 가
> 주는 번호가 서로 다릅니다. 둘 다 정상이며, 이 봇은 뒤엣것을 씁니다.

### 2-5. 최종 확인

`설정확인.bat` 실행 → 전부 **[정상]** 이면 끝입니다.

---

## 평소 사용법

| 하고 싶은 것 | 실행할 파일 |
|---|---|
| 초안 만들고 승인 화면 열기 | `실행.bat` |
| 초안은 그대로 두고 화면만 다시 열기 | `승인화면만.bat` |
| 설정이 제대로 됐는지 확인 | `설정확인.bat` |
| 토큰 60일 연장 (두 달에 한 번) | `토큰갱신.bat` |

### 성격 바꾸기

`config.json` 을 메모장으로 열어 고치면 됩니다. 코드는 안 건드려도 됩니다.

- **다루는 분야**: `분야 → 목록` 의 카테고리ID
  다른 분야 ID를 찾으려면: `.venv\Scripts\python.exe tools\find_category.py`
- **하루 몇 건**: `수집 → 하루_초안수`
- **말투**: `글투 → 지침` — 여기를 바꾸면 계정 색깔이 확 바뀝니다
- **카드 구성**: `카드구성 → 순서` — 여기 적은 개수가 곧 카드 장수입니다.
  고를 수 있는 종류는 `카드구성` 안에 설명이 같이 적혀 있습니다.

  | 종류 | 내용 |
  |---|---|
  | `훅` | 표지와 함께 나가는 첫 장. 항상 맨 앞에 두세요 |
  | `이야기` | 짧은 이야기 한 토막. 장면 → 흔들리는 순간 → 그래서 어떻게 됐는지 |
  | `상황` | "이런 적 있으시죠?" 하고 건네는 한 장면. 이야기보다 짧고 가볍습니다 |
  | `핵심` | 이 책이 주장하는 바 |
  | `질문` | 이 책이 답해주는 것들 (목차에서 뽑되 말하듯 풀어씁니다) |
  | `정리` | 어떤 사람에게 쓸모 있는지. 마지막 장에 두세요 |

  예: `["훅", "이야기", "이야기", "핵심", "정리"]` (기본값)

  **`이야기`를 연달아 두 장 이상 두면** 한 편의 이야기가 장면별로 나뉘어 이어집니다.
  각 장이 다음 장 궁금해지는 데서 끊기기 때문에 끝까지 넘겨보게 만드는 효과가 있습니다.
  세 장으로 늘리면 (장면 열기 → 조여들기 → 뒤집기) 구조가 됩니다.

- **카드 흐름**: `카드구성 → 흐름`

  | 값 | 뜻 |
  |---|---|
  | `이어지기` (기본값) | 카드 전체가 한 편의 글. 뒷장은 앞장을 읽었다고 치고 이어집니다 |
  | `묶음` | 카드마다 따로 읽혀도 되게. 한 장만 캡처해도 뜻이 통합니다 |

  > **`이야기`와 `상황` 카드에 대해**: 이건 봇이 만든 **일반적인 예시**이지 책에 나오는
  > 일화가 아닙니다. 책에 없는 사례를 있다고 쓰지 않도록 막아뒀고, 주인공도 '한 사람은',
  > '누군가는' 으로만 쓰게 했습니다('나는' 금지 — 실제 겪은 일처럼 읽히니까요).
  > 그래도 올리기 전에 한 번 읽어보세요.

- **말투**를 다시 딱딱하게 하고 싶으면 `글투 → 지침` 을 이렇게 바꾸세요:
  `"담백하고 단정적인 문체. 개조식 허용. 과장 광고 표현 금지. '~다' 체."`
- **품질을 올리고 싶으면**: `모델 → 이름` 을 `claude-sonnet-5` 로
  (비용이 몇 배 늘지만 그래도 월 몇천 원 수준입니다)

---

## 자주 나는 오류

**"토큰이 만료됐습니다" / code=190**
→ `토큰갱신.bat` 실행. 그래도 안 되면 2-3, 2-4의 토큰 생성 단계를 다시 하세요.
토큰은 60일마다 만료되고, 갱신도 60일 안에 해야 합니다. **두 달 넘게 안 돌리면 처음부터 다시** 받아야 합니다.

**"권한(scope)이 빠졌습니다" / code=10**
→ Meta 앱에서 `threads_content_publish` 또는 `instagram_business_content_publish` 권한이 빠졌습니다.

**인스타만 실패하고 쓰레드는 성공**
→ 계정이 아직 개인 계정입니다. 2-2를 다시 확인하세요.

**"이미지 처리가 2분 안에 끝나지 않았습니다"**
→ imgbb 업로드는 됐는데 Meta가 못 받아간 경우입니다. 잠시 후 다시 시도하세요.

**초안이 0건**
→ 이미 다룬 책은 다시 안 만듭니다. `config.json` 의 `최대_출간후_경과일` 을 늘리거나
분야를 추가하세요.

---

## 노출에 관해 (2026년 기준으로 확인된 것)

- **2025년 7월부터 구글이 인스타 공개 게시물을 색인합니다.** 프로 계정이면 검색에 뜹니다.
  그래서 **캡션 첫 줄에 실제 검색어**를 넣는 게 중요합니다. 봇이 자동으로 만들지만,
  승인 화면에서 직접 고칠 수 있게 해뒀습니다.
- **해시태그는 이제 거의 의미 없습니다.** 3~5개로 맞춰뒀습니다. 30개 도배는 손해입니다.
- **대체텍스트를 인스타 검색이 읽습니다.** 대부분 안 채우는데 봇이 카드마다 자동으로
  넣습니다. 승인 화면에서 펼쳐 볼 수 있습니다.
- **이미지 위의 큰 글자도 읽힙니다.** 카드뉴스 형식이 유리한 이유입니다.
- **⚠️ 새 계정이 처음부터 하루 여러 개를 규칙적으로 올리면 스팸으로 걸립니다.**
  그래서 `하루_초안수` 기본값을 **1건**으로 뒀습니다.
  2주쯤 사람처럼 쓰다가 천천히 늘리세요.

## 알라딘 OpenAPI 상위 등급 신청 (품질을 크게 올리는 유일한 길)

기본 등급으로는 **목차와 전체 소개글이 오지 않습니다.** 공식 매뉴얼에 이렇게 적혀 있습니다.

> 주황색 정보의 경우, 일반적인 스펙에는 포함되지 않고, **별도로 협의 후 제공**

`Toc`(목차), `fullDescription`(전체 소개글), `Story` 세 가지가 여기 해당합니다.
그래서 지금은 110~215자짜리 짧은 소개글만으로 글을 쓰고 있고, 이게 품질 천장입니다.

**승인되면 달라지는 것**
- 하루 호출 5,000회 → 100,000회
- **목차와 전체 소개글이 열립니다** ← 이게 핵심

**승인 조건** (전부 이 봇이 자동으로 만족시킵니다)

| 조건 | 봇의 대응 |
|---|---|
| `도서 DB 제공 : 알라딘 인터넷서점(www.aladin.co.kr)` 표기 | 캡션·쓰레드 본문·마지막 카드에 자동 삽입 |
| 알라딘 상품 페이지로 가는 링크 | 캡션과 쓰레드 본문에 자동 삽입 |
| 경쟁 서점 링크 없음 | 알라딘 외 링크를 넣지 않음 |
| 기본 API를 실제로 쓰고 있을 것 | 계정을 돌리는 것 자체가 증빙 |

`config.json` → `제휴 → 출처표기_사용` 이 `true` 인 동안 표기가 들어갑니다. **승인 전까지는 끄지 마세요.**

**신청 순서**

1. 인스타/쓰레드 계정을 만들고 **실제로 며칠 게시물을 올립니다**
   (신청서에 "기본 API를 쓰고 있다"는 증빙이 필요합니다)
2. TTB 관리 페이지에서 **등록 URL을 그 계정 주소로 바꿉니다**
   https://www.aladin.co.kr/ttb/wblog_manage.aspx → `정보수정`
3. 신청서 제출: **https://forms.gle/mUUYpoG3wfFo1vGn7**
4. 문의: **openapi@aladin.co.kr**

> 알라딘이 매년 조건 준수 여부를 점검합니다. 표기를 빼면 등급이 회수될 수 있습니다.

## 책 표지에 관해

책 표지는 **출판사 저작권 자산**입니다. 알라딘 제휴 회원으로 가입하면 쓸 근거가 생기지만,
완전히 안전한 길은 아닙니다. 판단은 직접 하시고, 설정으로 켜고 끌 수 있게 해뒀습니다.

`config.json` → `발행 → 표지_사용`
- `true` (기본값) — 첫 장에 알라딘이 주는 표지 이미지가 들어갑니다
- `false` — 표지 없이 제목·저자만 글자로 나갑니다 (타이포그래피 중심)

**줄거리와 핵심 아이디어를 요약하는 것 자체는 문제없습니다.** 사실과 아이디어에는
저작권이 없습니다. 문제가 되는 건 표지 이미지와 원문 긴 인용입니다.
이 봇은 소개글을 그대로 옮기지 않도록 만들어졌고, 겹치면 경고를 띄웁니다.

## 알아둘 것

- **하루 한도**: 인스타 100건, 쓰레드 250건. 정상적으로 쓰면 걸릴 일 없습니다.
- **토큰 60일**: 두 달에 한 번 `토큰갱신.bat` 을 돌려야 합니다. 달력에 반복 일정으로 넣어두세요.
- **`.env` 파일은 비밀번호입니다.** 이 폴더를 통째로 남에게 주거나 GitHub에 올리지 마세요.
- **저작권**: 이 봇은 출판사 소개글을 그대로 베끼지 않도록 만들어졌고, 베낀 부분이
  있으면 승인 화면에 경고가 뜹니다. 그래도 **올리기 전에 눈으로 한 번 읽어보세요.**
  AI가 쓴 문장이 사실과 다를 수 있습니다.
