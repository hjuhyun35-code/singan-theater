# 신간 극장 — 게시물 올리는 법

저장소: `hjuhyun35-code/singan-theater` · 계정: `@singan.theater`

> **카드뉴스 봇(news-bot)과 다릅니다.** 그쪽은 카드를 손으로 만들어 올렸지만
> 여기는 **초안이 저절로 만들어집니다.** 파일을 올릴 일이 없습니다.

---

## 매일 하는 일 (2분)

### 1. 텔레그램을 봅니다

아침 8시에 카드 5장과 문구가 옵니다. 메시지 아래에 슬러그가 적혀 있습니다.

```
올리려면 슬러그: 20260803-1
```

올릴 만하면 다음으로. 아니면 **그냥 무시하세요.** 초안은 쌓이기만 하고 사라지지 않습니다.

### 2. 예행연습 (선택, 안 해도 됨)

```
https://github.com/hjuhyun35-code/singan-theater/actions/workflows/publish.yml
```

오른쪽 `Run workflow ▾` → `slug` 에 슬러그 입력 → `confirm` 은 **그대로 두기** → 초록 `Run workflow`

이미지가 인스타에서 열리는지, 캡션이 어떻게 나가는지 보여주고 **발행 직전에 멈춥니다.**

### 3. 실제 발행

같은 주소에서 다시:

- `slug` → 같은 슬러그
- `confirm` → **`PUBLISH`** (전부 대문자, 앞뒤 공백 없이)

`PUBLISH` 철자가 하나라도 다르면 예행연습으로 돕니다. 안전장치입니다.

### 4. 확인

발행됐는지는 이 주소에서 확인됩니다. `"published": true` 면 실제로 올라간 것입니다.

```
https://raw.githubusercontent.com/hjuhyun35-code/singan-theater/main/posts/20260803-1/post.json
```

인스타 앱에서 눈으로 보는 게 가장 확실합니다.

---

## 초안을 지금 당장 만들고 싶을 때

```
https://github.com/hjuhyun35-code/singan-theater/actions/workflows/draft.yml
```

`Run workflow ▾` → `count` 에 숫자 (비우면 1건) → `Run workflow`

1~2분 뒤 텔레그램으로 옵니다.

---

## 뭔가 안 될 때 — 여기부터 보세요

### 인스타 점검

```
https://github.com/hjuhyun35-code/singan-theater/actions/workflows/ig-check.yml
```

`Run workflow ▾` → `Run workflow`

토큰이 살아 있는지, 발행 권한이 있는지 확인합니다. **아무것도 올리지 않습니다.**

| 결과 | 뜻과 조치 |
|---|---|
| `발행 준비 완료` | 정상. 다른 데 문제가 있는 것 |
| `토큰이 죽었거나 접근이 막혔습니다` | 토큰 재발급 (아래 참고) |
| `계정 종류가 ... 입니다` | 인스타에서 크리에이터 계정으로 전환 |

> ⚠️ **`API access blocked` 라고 나와도 대개 권한 문제가 아니라 토큰 만료입니다.**
> Meta 가 만료를 이 문구로 뭉뚱그려 표시합니다. 프로필 조회까지 막히면 토큰이 죽은 겁니다.

### 토큰 재발급

토큰은 **60일마다 만료**됩니다. 만료되면 발행이 조용히 멈춥니다.

1. Meta 대시보드에서 `glassnegative-bot` → Instagram 로그인이 포함된 API →
   `2. 액세스 토큰 생성` → `토큰 생성` → **즉시 복사** (한 번만 보여줍니다)
2. 아래 주소에서 `INSTAGRAM_ACCESS_TOKEN` 줄의 **연필 아이콘**을 눌러 수정
   ```
   https://github.com/hjuhyun35-code/singan-theater/settings/secrets/actions
   ```
3. `인스타 점검` 을 돌려 확인

### 초안이 0건으로 나올 때

워크플로는 초록불인데 텔레그램에 "새 초안이 없습니다" 만 올 때가 있습니다.

```
https://github.com/hjuhyun35-code/singan-theater/actions/workflows/draft.yml
```
실행 기록을 열어 `초안 생성` 단계 로그를 보세요.

- `후보 0권` + 403 → 알라딘이 막은 것. 잠시 후 다시 실행
- `이미 다룬 책` 뿐 → 정상. 분야를 늘리거나 하루 쉬면 됩니다

---

## 설정 바꾸기

`config.json` 을 고치면 봇의 성격이 바뀝니다. 코드는 안 건드려도 됩니다.

```
https://github.com/hjuhyun35-code/singan-theater/edit/main/config.json
```

| 바꿀 것 | 위치 |
|---|---|
| 다루는 분야 | `분야 → 목록` |
| 하루 초안 수 | `수집 → 하루_초안수` |
| 말투 | `글투 → 지침` |
| 카드 구성 | `카드구성 → 순서` |
| 책 표지 사용 여부 | `발행 → 표지_사용` |

고친 뒤 화면 아래 초록 `Commit changes...` → 팝업에서 **한 번 더** `Commit changes`.
두 번 눌러야 저장됩니다.

---

## 올린 뒤

- **댓글은 손으로 답하세요.** 자동 답글은 티가 납니다. 올린 지 1시간 안의 답글이 노출에 유리합니다
- **하루 하나면 충분합니다.** 같은 날 두 개를 올리면 서로 노출을 나눠 가집니다
- **올리는 시각을 조금씩 바꾸세요.** 매일 정확히 같은 시각은 기계처럼 보입니다
  (초안은 8시에 만들어지지만 발행은 손으로 하니 자연스럽게 달라집니다)
- **신규 계정은 2주쯤 하루 1개**로 가다가 천천히 늘리세요

---

## 건드리면 안 되는 것

- **Meta 개발자 앱 설정** — `glassnegative-bot` 은 `@theglassnegative` 와 공유합니다.
  하나 바꾸면 양쪽 계정에 영향이 갑니다
- **`confirm` 안전장치** — 워크플로에서 빼지 마세요
- **저장소를 비공개로 바꾸기** — 인스타가 카드 이미지를 못 가져가 발행이 실패합니다
- **토큰을 채팅에 붙여넣기** — 노출되면 즉시 재발급하세요

---

## 지금 남아 있는 숙제

- **토큰 자동 갱신이 없습니다.** 60일 뒤 수동 재발급이 필요합니다
- **쓰레드는 아직 연결 안 됨.** 인스타만 나갑니다
- **자료가 얇습니다.** 알라딘 기본 등급이라 소개글이 110~215자뿐입니다.
  게시물이 2~3주 쌓이면 [상위 등급](https://forms.gle/mUUYpoG3wfFo1vGn7)을 신청하세요.
  승인되면 목차와 전체 소개글이 열립니다
